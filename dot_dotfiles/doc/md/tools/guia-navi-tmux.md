# 🚀 GUÍA DE USO - NAVI + TMUX

## ACCESOS RÁPIDOS:

### TMUX:

| Atajo | Acción |
|---------|-------------|
| `Ctrl-b + d` | Salir de la sesión actual | 
| `Ctrl+b %` | Dividir verticalmente | 
| `Ctrl+b -` | Dividir horizontalmente  | 
| `Ctrl-b + flecha` | Mover a un panel diferente en una dirección | 
| `Ctrl-b + x` | Cerrar el panel actual | 
| `Ctrl-b + c` | Crear una nueva ventana | 
| `Ctrl-b + n` | Pasar a la siguiente ventana según el número | 
| `Ctrl-b + N` | Abrir navi (desde CUALQUIER app) | 
| `Ctrl-b + p` | Volver a la ventana anterior | 
| `Ctrl-b + número ID` | Saltar a una ventana concreta utilizando el número ID | 
| `Ctrl-b +` | Abrir el modo línea de comandos | 
| `Ctrl-b + ?` | Mostrar todos los atajosl | 
| `Ctrl-b + w` | Lista todas las ventanas de la sesión actua | 
| `Ctrl+b h/j/k/` | Navegar entre paneles| 
| `Ctrl+b H/J/K/L` | Redimensionar paneles| 

### NAVI:

- En terminal: `navi` -> Buscar comandos
- En cualquier app: `Ctrl+A N` -> Abrir navi

## ESTRUCTURA RECOMENDADA:

1. Panel izquierdo: Guía/documentación (nano, vim)
2. Panel derecho: Ejecutar comandos
3. Usar `Ctrl+A N` para buscar comandos sin salir del editor

## PRIMEROS PASOS RECOMENDADOS:

1. Crear guías para tus tareas frecuentes
2. Convertir comandos complejos a cheatsheets de navi
3. Practicar la navegación con tmux
